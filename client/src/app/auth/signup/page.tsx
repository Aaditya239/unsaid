import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - UNSAID',
  description: 'Create your UNSAID account',
};

export default function SignupPage() {
  return <SignupForm />;
}
