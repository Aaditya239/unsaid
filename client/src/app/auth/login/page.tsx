import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - UNSAID',
  description: 'Sign in to your UNSAID account',
};

export default function LoginPage() {
  return <LoginForm />;
}
